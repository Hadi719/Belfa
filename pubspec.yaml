name: apach
description: "A new Flutter project."
publish_to: "https://github.com/Hadi719/Apach/releases"
platforms:
  android:

version: 0.0.1+1

# Created with flutter 3.24.1
environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.6
  logging: ^1.2.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  build_runner: ^2.4.6
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true
